# Optional: override API base (required if you use `expo start --tunnel` — Metro is tunneled but your API is not).
# Use your LAN IP for same-WiFi phone + laptop, or your deployed URL:
# EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
# EXPO_PUBLIC_API_URL=https://your-api.onrender.com
